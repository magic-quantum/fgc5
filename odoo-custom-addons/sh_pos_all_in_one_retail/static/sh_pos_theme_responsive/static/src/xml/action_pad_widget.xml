<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
	
	<t t-name="ActionpadWidget" t-inherit="point_of_sale.ActionpadWidget" t-inherit-mode="extension" owl="1">
		<!-- <xpath expr="//button[hasclass('pay')]" position="after"> -->
        <xpath expr="//button[hasclass('back-button')]" position="after">
			<button class="button toggle" t-on-click="sh_hide_numpad">
				<div class="slide_toggle_button">
					<i class="fa fa-chevron-down" aria-hidden="true"></i>
                </div>
            </button>
		</xpath>
		<xpath expr="//button[hasclass('back-button')]" position="replace">
			<div t-if="env.isMobile" class="back-button" ></div>
		</xpath>
	</t>
	
	<t t-name="NumpadWidget" t-inherit="point_of_sale.NumpadWidget" t-inherit-mode="extension" owl="1">
		<xpath expr="//div[hasclass('numpad')]" position="replace">
            <div class="numpad">
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('1')">1</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('2')">2</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('3')">3</button>
                <button class="mode-button" t-att-class="{'selected-mode': props.activeMode === 'quantity', 'disabled-mode': disable_quantity}"
                        t-on-click="() => this.changeMode('quantity')" t-att-disabled="disable_quantity">Qty</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('4')">4</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('5')">5</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('6')">6</button>
                <button class="mode-button" t-att-class="{
                            'selected-mode': props.activeMode === 'discount',
                            'disabled-mode': !hasManualDiscount or disable_discount
                        }"
                        t-att-disabled="!hasManualDiscount or disable_discount"
                        t-on-click="() => this.changeMode('discount')">% Disc</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('7')">7</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('8')">8</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('9')">9</button>
                <button class="mode-button" t-att-class="{
                        'selected-mode': props.activeMode === 'price',
                        'disabled-mode': !hasPriceControlRights or disable_price
                    }" t-att-disabled="!hasPriceControlRights or disable_price"
                        t-on-click="() => this.changeMode('price')" >Price</button>
                <button class="input-button numpad-minus" t-att-class="{ 'disabled-mode': props.disableSign or plusminus}" t-att-disabled="plusminus" t-on-click="() => this.sendInput('-')">+/-</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput('0')">0</button>
                <button class="input-button number-char" t-att-class="{'disabled-mode': numpad}" t-att-disabled="numpad" t-on-click="() => this.sendInput(decimalSeparator)">
                    <t t-esc="decimalSeparator" />
                </button>
                <button class="input-button numpad-backspace" t-att-class="{ 'disabled-mode': remove_button}" t-att-disabled="remove_button" t-on-click="() => this.sendInput('Backspace')">
                    <img style="pointer-events: none;" src="/point_of_sale/static/src/img/backspace.png"
                         width="24" height="21" alt="Backspace" />
                </button>
            </div>
		</xpath>
	</t>
	
</templates>